; GTA camera wrappers.

macro SET_CAMERA_BEHIND_PLAYER() {
    CALL "GTA_SetCameraBehindPlayer"
}

macro SET_FIXED_CAMERA_POSITION(x, y, z, rx, ry, rz) {
    CALL "GTA_SetFixedCameraPosition" x y z rx ry rz
}

macro RESTORE_CAMERA() {
    CALL "GTA_RestoreCamera"
}
